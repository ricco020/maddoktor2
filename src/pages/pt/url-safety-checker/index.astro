---
import Base from '../../../layouts/Base.astro';
import { SITE } from '../../../lib/site';
import { hreflangAlternates } from '../../../lib/hreflang';

const title = "Verificador de URL - Detete sinais de phishing num link";
const description = "Verificador de URL gratuito. Cole um link suspeito e a ferramenta inspeciona o endereco a procura de sinais de phishing comuns, tudo no seu navegador. Nao garante que um site e seguro, apenas assinala avisos.";

const alternates = hreflangAlternates('/url-safety-checker/');

const MSG = {"empty": "Cole um link para o verificar.", "invalid": "Isso nao parece uma URL valida.", "verdictClean": "Sem sinais comuns no endereco. Ainda assim nao e garantia de seguranca.", "verdictInfo": "Alguns pontos a notar neste link.", "verdictWarn": "Alguns sinais de aviso - cuidado com este link.", "verdictBad": "Sinais fortes - trate este link como suspeito.", "disclaimer": "Esta ferramenta apenas inspeciona o texto da URL a procura de sinais comuns. Nao pode confirmar se um site e seguro ou malicioso. Na duvida, nao clique.", "msg": {"notHttps": "Este link usa http, nao https, por isso o trafego pode ser lido ou alterado pelo caminho.", "ipHost": "O endereco e um IP em bruto, nao um nome de dominio - comum em phishing, raro em marcas reais.", "atSymbol": "A URL contem '@'. Tudo o que esta antes e ignorado pelo navegador - um truque classico para ocultar o destino real.", "punycode": "O dominio usa punycode (xn--) e pode aparecer como imitacao de um nome conhecido.", "manySub": "O dominio tem muitos subdominios. Um nome de marca num subdominio nao o torna essa marca.", "shortener": "E um encurtador de links - o destino real fica oculto ate clicar.", "riskyTld": "O dominio de topo e dos que sao frequentemente abusados. Nao e prova, mas cuidado.", "hyphens": "O dominio tem varios hifenes, muitas vezes usados para criar nomes imitados.", "port": "A URL usa uma porta nao padrao, invulgar em sites normais.", "encodedHost": "O anfitriao contem caracteres codificados em percentagem, por vezes usados para ocultar o dominio real.", "longUrl": "A URL e muito longa, o que pode esconder o dominio real da vista.", "brandLookalike": "Um nome de marca conhecida aparece no endereco mas nao como dominio principal - um padrao de falsificacao comum.", "clean": "Nenhum sinal estrutural comum encontrado. Isto nao significa que o site seja seguro, apenas que o endereco parece comum."}};

const schema = [
  {
    '@context': 'https://schema.org',
    '@type': 'SoftwareApplication',
    name: "Verificador de URL",
    applicationCategory: 'SecurityApplication',
    operatingSystem: 'Any (web browser)',
    description,
    url: `${SITE.url}/pt/url-safety-checker/`,
    inLanguage: 'pt',
    offers: { '@type': 'Offer', price: '0', priceCurrency: 'USD' },
    publisher: { '@id': `${SITE.url}/#org` },
  },
  {
    '@context': 'https://schema.org',
    '@type': 'BreadcrumbList',
    itemListElement: [
      { '@type': 'ListItem', position: 1, name: "Inicio", item: SITE.url },
      { '@type': 'ListItem', position: 2, name: "Verificador de URL", item: `${SITE.url}/pt/url-safety-checker/` },
    ],
  },
];
---

<Base {title} {description} {schema} lang="pt" {alternates} canonicalPath="/pt/url-safety-checker/">
  <article class="prose rise mx-auto">
    <p class="mono text-xs text-ink-faint">~ / url-safety-checker</p>
    <h1 class="mt-4 font-mono text-2xl font-semibold text-ink sm:text-3xl">Verificador de URL</h1>

    <p class="mt-6 text-lg text-ink-dim" set:html={"Cole um link suspeito abaixo. Esta ferramenta inspeciona <strong>o proprio endereco</strong> a procura de sinais comuns de phishing e malware - IP em bruto, nomes de marca imitados, punycode, encurtadores arriscados e mais. Funciona <strong>inteiramente no seu navegador</strong>, nunca visita o link nem o envia para lado nenhum."}></p>

    <div id="urltool" class="urltool">
      <div class="urlrow">
        <input id="urlinput" type="text" inputmode="url" autocomplete="off" spellcheck="false" placeholder="Cole uma URL, ex. http://paypal.secure-login.example.tk/verify" />
        <button id="analyze" type="button">Verificar o link</button>
      </div>
      <p id="verdict" class="verdict" role="status" aria-live="polite"></p>
      <div id="results"></div>
    </div>

    <h2>O que procura</h2>
    <p>Assinala indicios estruturais no endereco: falta de HTTPS, um IP em vez de um dominio, um @ que oculta o destino real, imitacoes em punycode, subdominios profundos, encurtadores de links, dominios de topo frequentemente abusados, portas invulgares, caracteres codificados e nomes de marcas conhecidas usados fora do dominio principal.</p>

    <h2>Os limites honestos</h2>
    <p set:html={"E uma verificacao de texto, nao um veredicto. Nao consegue abrir a pagina, executar uma analise nem consultar uma base de ameacas, por isso um resultado limpo <strong>nao</strong> prova que um site e seguro, e um aviso nao prova que e malicioso. Encare os avisos como motivo para abrandar. Na duvida, nao clique - escreva o endereco voce mesmo ou abra a app oficial."}></p>
    <p class="related-tool"><strong>Ferramenta relacionada:</strong> <a href="/pt/file-hash-checker/">Verificador de hash de ficheiro</a></p>
  </article>
</Base>

<style>
  .urltool { margin: 26px 0; }
  .urlrow { display: flex; gap: 8px; flex-wrap: wrap; }
  .urlrow input {
    flex: 1 1 260px; min-width: 0; padding: 12px 14px; font-family: ui-monospace, monospace;
    font-size: 14px; border-radius: 10px; border: 1px solid var(--line, #33333c);
    background: rgba(255,255,255,0.02); color: inherit;
  }
  .urlrow input:focus { outline: none; border-color: #22c55e; }
  .urlrow button {
    padding: 12px 20px; border-radius: 10px; border: 1px solid #22c55e; cursor: pointer;
    background: rgba(34,197,94,0.12); color: #22c55e; font-weight: 600;
  }
  .urlrow button:hover { background: rgba(34,197,94,0.2); }
  .verdict { margin: 18px 2px 8px; font-weight: 700; min-height: 22px; }
  .v-clean { color: #22c55e; } .v-info { color: #38bdf8; } .v-warn { color: #f59e0b; } .v-bad { color: #ef4444; }
  .findings { list-style: none; padding: 0; margin: 8px 0; display: flex; flex-direction: column; gap: 8px; }
  .findings li {
    padding: 10px 12px 10px 14px; border-radius: 10px; font-size: 14px; line-height: 1.45;
    border: 1px solid var(--line, #33333c); border-left-width: 4px; background: rgba(255,255,255,0.02);
  }
  .f-bad { border-left-color: #ef4444; } .f-warn { border-left-color: #f59e0b; }
  .f-info { border-left-color: #38bdf8; } .f-clean { border-left-color: #22c55e; }
  .disclaimer { font-size: 12.5px; opacity: .7; margin-top: 14px; }
</style>


<script is:inline define:vars={{ MSG: MSG }}>
  const el = document.getElementById('urltool');
  if (el) {
    const input = document.getElementById('urlinput');
    const btn = document.getElementById('analyze');
    const out = document.getElementById('results');
    const verdict = document.getElementById('verdict');
    const SHORTENERS = ['bit.ly','tinyurl.com','goo.gl','t.co','ow.ly','is.gd','buff.ly','rebrand.ly','cutt.ly','t.ly','rb.gy','shorturl.at'];
    const RISKY_TLDS = ['zip','mov','tk','gq','ml','cf','ga','top','xyz','click','country','kim','work','loan','support','rest'];
    const BRANDS = ['paypal','apple','icloud','microsoft','office365','amazon','netflix','google','facebook','instagram','whatsapp','bank','coinbase','binance','metamask','dhl','fedex','ups','usps'];

    function analyze(raw) {
      const findings = [];
      let value = (raw || '').trim();
      if (!value) return null;
      if (!/^[a-z][a-z0-9+.-]*:\/\//i.test(value)) value = 'http://' + value;
      let u;
      try { u = new URL(value); } catch (e) { return { error: true }; }
      const host = u.hostname.toLowerCase();
      const labels = host.split('.');
      const tld = labels[labels.length - 1] || '';
      const registrable = labels.slice(-2).join('.');

      if (u.protocol === 'http:') findings.push({ s: 'warn', k: 'notHttps' });
      if (/^\d{1,3}(\.\d{1,3}){3}$/.test(host)) findings.push({ s: 'bad', k: 'ipHost' });
      if (u.username || u.password || raw.split('#')[0].split('?')[0].includes('@')) findings.push({ s: 'bad', k: 'atSymbol' });
      if (host.includes('xn--')) findings.push({ s: 'bad', k: 'punycode' });
      if (labels.length > 4) findings.push({ s: 'warn', k: 'manySub' });
      if (SHORTENERS.includes(registrable)) findings.push({ s: 'warn', k: 'shortener' });
      if (RISKY_TLDS.includes(tld)) findings.push({ s: 'warn', k: 'riskyTld' });
      if ((registrable.match(/-/g) || []).length >= 2) findings.push({ s: 'warn', k: 'hyphens' });
      if (u.port) findings.push({ s: 'warn', k: 'port' });
      if (host.includes('%')) findings.push({ s: 'bad', k: 'encodedHost' });
      if (value.length > 100) findings.push({ s: 'info', k: 'longUrl' });
      const brandHit = BRANDS.find((b) => host.includes(b));
      if (brandHit && registrable.indexOf(brandHit) !== 0) findings.push({ s: 'bad', k: 'brandLookalike' });
      return { host, findings };
    }

    function render(raw) {
      out.replaceChildren();
      verdict.textContent = '';
      verdict.className = 'verdict';
      const res = analyze(raw);
      if (!res) { verdict.textContent = MSG.empty; return; }
      if (res.error) { verdict.textContent = MSG.invalid; verdict.classList.add('v-bad'); return; }
      const findings = res.findings;
      const worst = findings.some((f) => f.s === 'bad') ? 'bad' : findings.some((f) => f.s === 'warn') ? 'warn' : findings.length ? 'info' : 'clean';
      verdict.classList.add('v-' + worst);
      verdict.textContent = worst === 'clean' ? MSG.verdictClean
        : worst === 'bad' ? MSG.verdictBad
        : worst === 'warn' ? MSG.verdictWarn : MSG.verdictInfo;

      const list = document.createElement('ul');
      list.className = 'findings';
      if (!findings.length) {
        const li = document.createElement('li');
        li.className = 'f-clean';
        li.textContent = MSG.msg.clean;
        list.appendChild(li);
      } else {
        findings.forEach((f) => {
          const li = document.createElement('li');
          li.className = 'f-' + f.s;
          li.textContent = MSG.msg[f.k];
          list.appendChild(li);
        });
      }
      out.appendChild(list);
      const note = document.createElement('p');
      note.className = 'disclaimer';
      note.textContent = MSG.disclaimer;
      out.appendChild(note);
    }

    btn.addEventListener('click', () => render(input.value));
    input.addEventListener('keydown', (e) => { if (e.key === 'Enter') render(input.value); });
  }
</script>
